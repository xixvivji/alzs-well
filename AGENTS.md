# ALZ's well Agent Rules

이 파일은 저장소 전체에 적용된다. 특히 고객·행원·관리자용 화면을 만들거나 변경할 때 아래 UX/Product Design 규칙을 구현 판단보다 먼저 적용한다.

## Product truth and source priority

충돌이 있으면 다음 순서로 제품 사실을 판단한다.

1. `ALZS_WELL_PROJECT_SSOT.md`
2. `frontend/PRODUCT.md`
3. `frontend/DESIGN.md`
4. `docs/FRONTEND_API_ROLE_MATRIX.md`, `docs/DEMO_REHEARSAL.md`, `docs/UX_ACCESSIBILITY_VALIDATION.md`
5. 현재 구현

ALZ's well은 치매·인지저하·사기 여부를 진단하거나 위험 고객을 분류하는 서비스가 아니다. 고객 개인의 장기 금융생활 기준선에서 설명할 수 있는 변화를 찾고, 고객에게 먼저 맥락을 확인한 뒤, 고객이 선택한 도움과 행원의 검토로 연결하는 금융생활 연속성 서비스다.

고령자와 기억·주의·판단 부담이 커질 수 있는 사용 환경을 기본 설계 조건으로 삼되, 나이·행동·화면 설정을 근거로 사용자의 질병이나 인지상태를 추론하거나 UI를 몰래 축소하지 않는다. 접근성 지원은 낙인이 아니라 모든 사용자가 직접 선택할 수 있는 기본 기능이다.

## UX / Product Design rules

### 1. Start with the decision, not the styling

사용자 화면을 만들거나 바꾸기 전에 다음을 먼저 한 문장씩 정의한다.

- 이 화면의 주 사용자는 누구인가?
- 사용자가 이 화면에 들어온 이유는 무엇인가?
- 화면을 나갈 때 완료되어야 하는 한 가지 일은 무엇인가?
- 제품이 달성해야 하는 목표는 무엇인가?
- 사용자가 결정 전에 반드시 알아야 하는 사실은 무엇인가?
- 사용자가 결정을 미루거나 도움을 요청할 수 있는가?

이 답이 없으면 컴포넌트, 색상, 카드 배치부터 만들지 않는다. 화면 구조는 사용자의 결정 순서를 따라야 한다.

### 2. The user's first task

서비스 전체에서 고객의 가장 첫 번째 과업은 **본인의 금융생활 의향과 도움받을 범위를 이해하고 직접 확인하는 것**이다. 알림 수신, 신뢰연락인 공유, 보호서비스 선택보다 먼저 다음 내용을 고객이 확인할 수 있어야 한다.

- 유지하고 싶은 필수 금융생활
- 선호하는 설명 방식과 접근성 설정
- 도움을 받고 싶은 조건
- 공유할 정보, 공유 대상, 공유 시점
- 언제든 동의하거나 철회할 수 있다는 사실

변화 알림을 받은 뒤의 첫 번째 과업은 **조치가 아니라 이해와 맥락 확인**이다. 먼저 무엇이 평소와 얼마나 달라졌는지 이해하게 하고, 그다음 고객이 `알고 한 활동`, `생활 변화가 있었음`, `확인하기 어려움`, `내가 하지 않은 활동`, `나중에 확인`, `은행에 문의` 중 자신의 맥락을 선택하게 한다.

고객 응답을 받기 전에 상품, 보호서비스, 신뢰연락인 연락 또는 직원 조치를 먼저 밀어붙이지 않는다.

### 3. Flow before layout

각 화면은 구현 전에 다음 흐름을 정의한다.

1. 진입점과 진입 이유
2. 현재 상태를 설명하는 사실
3. 사용자가 내려야 하는 한 가지 결정
4. Primary path
5. 미루기·도움 요청·취소를 포함한 secondary path
6. loading, empty, error, blocked, success 상태
7. 결정 이후 다음 화면 또는 담당자에게 이어지는 폐루프

고객 확인 흐름은 원칙적으로 한 화면에 질문 하나만 둔다. 여러 결정을 한 화면에서 동시에 요구하지 않는다. 중단 후 돌아와도 이전 선택과 현재 단계를 복원하고, 처음부터 다시 기억하게 만들지 않는다.

### 4. Information hierarchy

정보는 다음 네 단계로 분류한다.

1. **Primary:** 지금 이해해야 할 변화, 현재 질문, 다음 행동
2. **Secondary:** 결정을 돕는 금액·횟수·기간·비교 기준과 영향
3. **Supporting:** 근거 출처, 동의 상태, 도움 옵션, 후속 일정
4. **Optional:** 알고리즘 버전, 사유코드, hash, 원시 이벤트와 감사 상세

모든 정보에 같은 시각적 무게를 주지 않는다. 카드 개수를 정보 구조로 착각하지 말고, 관련 사실은 한 덩어리로 묶는다. 색상이나 아이콘만으로 중요도와 상태를 전달하지 않는다.

### 5. Primary CTA

한 작업 영역에는 Primary CTA를 하나만 둔다. Primary CTA는 서비스가 원하는 행동이 아니라 **사용자가 현재 결정을 안전하게 완료하는 행동**이다.

- 준비·온보딩: `내 금융생활 의향 확인하기` 또는 `안심 준비 시작하기`
- 변화 설명: `변화 내용 확인하기`
- 고객 맥락 확인: 현재 질문에 대한 고객의 선택 자체. 어느 응답도 정답처럼 더 강하게 강조하지 않는다.
- 도움 선택: `필요한 도움 선택하기`
- 행원 사건 화면: `검토 시작`
- 행원 검토 완료: `안내 계획 승인`. 이는 상담 계획 승인일 뿐 실제 계좌 조치가 아님을 인접 문장으로 표시한다.

Primary CTA는 사용자가 필요한 정보를 읽고 자연스럽게 결정하는 지점에 배치한다. 모바일에서 화면 하단에 고정할 수 있지만 핵심 설명을 가리면 안 된다. Secondary action은 낮은 시각적 강조를 사용하되 `나중에 확인`, `은행에 문의`, `취소`를 숨은 메뉴에 넣지 않는다.

위험하거나 되돌리기 어려운 행동은 Primary CTA와 시각적으로 분리한다. 서로 다른 결과의 버튼을 모두 primary 스타일로 만들지 않는다.

### 6. Required information before a decision

고객에게 선택을 요구하기 전에 최소한 다음을 쉬운 문장으로 보여준다.

1. **무엇이 달라졌는가:** 개인의 평소값과 최근값을 금액·횟수·기간으로 비교
2. **왜 묻는가:** 질병이나 사기 판정이 아니라 금융생활 변화의 맥락 확인임을 설명
3. **무엇이 확인되었고 무엇이 미확인인가:** 사실, 고객 응답, 구조적 근거를 구분
4. **선택하면 무엇이 일어나는가:** 저장·공유·직원 연결·후속 확인 여부
5. **무엇이 자동으로 일어나지 않는가:** 자동 송금, 지급정지, 상품 가입, 가족 연락, 건강 판단이 없음을 명시
6. **다른 선택:** 미루기, 취소, 은행에 문의하기

동의가 필요한 경우 공유 항목, 공유 대상, 목적, 유효기간 또는 철회 방법을 확인 전에 보여준다. 결과나 불이익을 버튼을 누른 뒤에 처음 공개하지 않는다.

### 7. Beginner and professional disclosure

사용자의 나이 또는 추정 인지상태가 아니라 **역할과 과업**에 따라 정보 밀도를 조절한다.

#### Customer default

- 결론보다 `평소와 최근의 차이`와 `지금 할 일`을 먼저 보여준다.
- 한 문단에는 한 메시지만 쓰고, 전문 용어 대신 일상어를 사용한다.
- 금액, 날짜, 횟수, 상태처럼 결정에 필요한 사실은 접지 않는다.
- 사유코드, 알고리즘 버전, 상세 근거는 `자세히 보기`에서 점진적으로 공개한다.
- 큰 글씨·고대비·쉬운 금융 모드는 사용자가 직접 켜고 끌 수 있게 하며 같은 사실과 선택지를 유지한다.

#### Staff default

- 고객 응답, 확인된 맥락, 미확인 항목, 평소값과 현재값, 근거, 의향, 다음 업무를 한 화면에서 스캔할 수 있게 한다.
- AI 생성 내용에는 `AI 초안`, 근거 출처, 폴백 여부, 사람 검토 필요 상태를 명확히 표시한다.
- 원시 payload와 기술 식별자는 기본 업무 흐름 아래의 상세 영역에 둔다.

#### Admin and audit default

- 정책·알고리즘·버전·권한·감사·보존 상태를 조밀하게 보여줄 수 있다.
- 요약에서 상세 근거까지 추적 가능해야 하며, 기술 정보를 고객 화면으로 그대로 복사하지 않는다.

Progressive disclosure는 핵심 위험, 비용, 공유 범위 또는 결정 결과를 숨기는 수단으로 사용하지 않는다.

### 8. Risk and confirmation levels

확인 단계는 무조건 늘리지 않는다. 기억 부담을 높이는 반복 팝업 대신 행동의 영향에 맞춘 의미 있는 단계를 사용한다.

- **Level 0 — 조회·탐색:** 확인 없음.
- **Level 1 — 되돌릴 수 있는 개인 설정·임시 저장:** 한 번의 명시적 저장. 가능하면 실행 후 되돌리기 제공.
- **Level 2 — 동의 부여, 정보 공유 범위 변경, 신뢰연락인 활성화, 행원의 안내 계획 승인:** `영향과 범위 검토 → 명시적 확인`의 2단계. 필요할 때만 재인증을 결합한다.
- **동의 철회·도움 거절:** 고객 권리 행사를 방해하는 추가 단계를 만들지 않는다. 결과를 한 번 명확히 알리고 확인하며, 법적·기술적으로 가능하면 복구 경로를 제공한다.
- **Level 3 — 실제 송금, 지급정지, 한도 변경, 상품 가입, 외부 연락 등 금융·대외 실행:** 현재 MVP에서 구현하지 않는다. 모의 버튼을 실제 실행처럼 보이게 만들지 말고, 향후 구현 시 은행의 별도 인증·권한·사람 승인 절차로 넘긴다.

Destructive action은 기본 포커스를 받지 않으며, 결과·대상·범위를 구체적인 명사와 수치로 다시 보여준다. `확인하시겠습니까?`만 반복하는 빈 확인창은 사용하지 않는다.

### 9. Cognitive load and accessibility

- 고객용 주요 버튼과 입력은 최소 48px 높이를 권장하고 어떤 주요 조작 영역도 44px 미만으로 만들지 않는다.
- 레이블을 placeholder나 아이콘만으로 대체하지 않는다.
- 선택지를 기억해서 비교하게 하지 말고 화면에 동시에 명확한 문장으로 제시한다.
- 시간 제한을 기본으로 두지 않는다. 보안상 세션 만료가 필요하면 만료 전에 알리고 안전한 연장·복구 경로를 제공한다.
- 진행 중인 단계, 저장 여부, 다음 행동을 텍스트로 표시한다.
- 오류는 원인보다 `무엇을 유지했고 어떻게 계속하는지`를 먼저 설명한다.
- 성공 화면은 완료된 일, 완료되지 않은 일, 다음 단계를 함께 확인해 준다.
- 자동 재생, 과도한 움직임, 깜박임, 불필요한 카운트다운을 사용하지 않는다. `prefers-reduced-motion`을 존중한다.
- 키보드 포커스, landmark, `aria-current`, `aria-live`, 오류 알림을 유지한다.
- 실제 고령 사용자 조사나 임상 검증을 하지 않았다면 효과를 검증했다고 표현하지 않는다.

### 10. Language and trust

고객 문구는 짧고, 중립적이며, 비낙인적이어야 한다.

- 사용: `평소와 다른 금융생활 변화`, `확인이 필요한 변화`, `쉬운 금융 모드`, `은행에 문의하기`
- 금지: `치매 의심`, `인지저하 확률`, `고위험 고객`, `이상한 행동`, 근거 없는 `사기 의심`

공포, 수치심, 조급함으로 행동을 유도하지 않는다. `즉시`, `위험`, `차단` 같은 표현은 검증된 긴급성과 실제 결과가 있을 때만 쓴다. 고객이 선택하지 않은 도움을 추천처럼 반복 노출하지 않는다.

### 11. Desktop and mobile priority

데스크톱과 모바일은 레이아웃이 달라도 **정보와 행동의 순서가 같아야 한다**.

1. 화면 목적과 현재 상태
2. 변화 또는 결정에 필요한 핵심 사실
3. 사용자의 질문 또는 선택
4. Primary CTA
5. 미루기·문의·취소 등 secondary path
6. 상세 근거와 기술 정보

모바일에서는 단일 열과 선형 순서를 기본으로 하고, 표는 핵심 필드가 먼저 읽히는 행 카드로 바꾼다. 가로 스크롤을 기본 해법으로 사용하지 않는다. 데스크톱의 오른쪽 패널이나 hover에만 있던 핵심 행동을 모바일의 숨은 메뉴로 옮기지 않는다.

반응형 변경 후에도 CTA 문구, 위험 수준, 확인 단계, 상태 의미를 바꾸지 않는다. 시각적 순서와 DOM·스크린리더 순서가 일치해야 한다.

### 12. Tool responsibilities

#### Impeccable

정보 위계, CTA 위치, 내비게이션, 인지부담, 접근성, 반응형 구조를 결정하거나 완성 화면을 점검할 때 사용한다. 시각 작업 전에 `frontend/PRODUCT.md`와 `frontend/DESIGN.md`를 읽고, 완료 전에 해당 화면을 audit 또는 detector로 검증한다.

#### daisyUI

UX 흐름과 의미 구조를 먼저 확정한 뒤 버튼, 입력, 알림, 모달 같은 재사용 컨트롤을 구현할 때 사용한다. 기존 daisyUI 컴포넌트를 우선 검토하되 다음을 지킨다.

- Tailwind 유틸리티는 주로 레이아웃과 간격에 사용한다.
- `frontend/DESIGN.md`의 색상, 크기, 모서리, 포커스 규칙을 daisyUI 기본 테마보다 우선한다.
- 컴포넌트의 기본 외형 때문에 정보 위계나 CTA 의미를 바꾸지 않는다.
- 접근 가능한 HTML 의미와 기존 동작을 보존한다.

#### MagicPath

새로운 주요 화면, 복수의 타당한 레이아웃, 복잡한 상호작용 흐름처럼 시각 비교가 실제 의사결정에 도움이 될 때 최종 구현 전에 사용한다. 탐색 결과를 바로 제품 코드로 확정하지 말고 다음을 확인한다.

- 어떤 안이 고객의 결정 순서를 가장 잘 보존하는가?
- 모바일과 큰 글씨에서 같은 우선순위가 유지되는가?
- 기존 기능, 데이터 흐름, 접근성, 에러 상태가 보존되는가?

MagicPath 결과를 가져올 때는 고정 폭, 가짜 데이터, 빈 이벤트 핸들러, 누락된 loading/error/empty 상태를 그대로 두지 않는다.

### 13. UX review before completion

사용자 화면을 완료하기 전에 다음을 확인한다.

- 첫 방문자가 5초 안에 화면 목적과 현재 할 일을 말할 수 있는가?
- Primary CTA가 하나이며 결정 지점에 있는가?
- 고객이 결정 전에 변화, 근거, 결과, 자동으로 일어나지 않는 일을 알 수 있는가?
- `나중에 확인`과 `은행에 문의` 경로가 눈에 보이는가?
- 고객의 응답과 구조적 근거를 혼동하지 않는가?
- AI 초안과 사람의 최종 판단을 구분하는가?
- 동의하지 않은 정보와 기능이 UI와 서버 모두에서 차단되는가?
- loading, empty, error, blocked, success 상태가 있는가?
- 중단·오류 후 입력과 진행 상태가 보존되는가?
- 큰 글씨, 고대비, 키보드, 스크린리더에서 동일한 과업을 끝낼 수 있는가?
- 모바일이 데스크톱과 같은 정보·행동 우선순위를 유지하는가?
- 질병 추론, 위험 낙인, 검증되지 않은 성과 주장을 만들지 않았는가?
- 합성 데이터와 외부 금융 실행 없음의 경계를 필요한 위치에 표시했는가?

하나라도 충족하지 못하면 화면은 완료된 것으로 간주하지 않는다.
