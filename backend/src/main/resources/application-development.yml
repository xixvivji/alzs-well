app:
  cors:
    customer-allowed-origins: ${CORS_CUSTOMER_ALLOWED_ORIGINS:http://localhost:5173}
    staff-allowed-origins: ${CORS_STAFF_ALLOWED_ORIGINS:http://localhost:4173}
    require-https: false
  demo:
    staff-bootstrap-username: ${DEMO_STAFF_USERNAME:demo-staff}
    staff-bootstrap-password: ${DEMO_STAFF_PASSWORD:local-demo-staff-password-change-me-1234567890}
